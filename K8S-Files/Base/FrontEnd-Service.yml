apiVersion: v1
kind: Service
metadata:
  name: hms-frontend-svc
  namespace: prod
spec:
  selector:
    app: hms-frontend-pod
  ports:
  - protocol: TCP
    port: 80      
    targetPort: 80 
  type: loadBalancer 